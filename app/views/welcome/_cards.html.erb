<h3 class="title"><%= t("welcome.cards.title") %></h3>

<div class="row welcome-cards">
  <div class="small-12">
    <% @cards.each do |card| %>
      <%= render "card", card: card %>
    <% end %>
  </div>
</div>
